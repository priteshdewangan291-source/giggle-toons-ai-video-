# Giggle Toons AI Video Studio

Simple phone-friendly Node.js app. Upload `server.js`, `package.json`, and `.env.example` to your hosting service.

Set the secret environment variable `OPENAI_API_KEY` on the server. Never put your real API key in GitHub.
